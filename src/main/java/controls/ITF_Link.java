package controls;

import controls.baseControls.BaseControl;
import core.BaseDriver;
import org.openqa.selenium.By;

public class ITF_Link extends BaseControl {
    public ITF_Link(BaseDriver driver, By locator) { super(driver, locator);}
}
